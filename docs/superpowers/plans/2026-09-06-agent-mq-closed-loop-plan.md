# Agent RAG 与 MQ 闭环实施计划

对应规格：`../specs/2026-09-06-agent-mq-closed-loop-spec.md`。

- [x] P0：检查分支和已有实现，先落规格与计划；不改 Spring AI/MCP/前端。
- [x] P1：添加 V5 通知迁移及 MySQL 持久化组件；将 MQ 消费改成幂等落库，保留失败消息。
- [x] P2：修复 Outbox 认领代数 fencing 和批次租约窗口；测试确认/失败/重复投递语义。
- [x] P3：实现用户隔离的状态、通知、已读接口；注册 Agent 只读查询工具，补齐规则回退。
- [x] P4：输出预约草稿 SSE，修复历史序列化/缓存降级、无匹配知识证据问题；保留部分工具结果并共用回退预算。
- [x] P5：增加离线回归与闭环测试，执行编译和限定单测；不启动项目及中间件。
- [x] P6：补充接口、迁移、故障重放和验证报告；审查 diff，只交付实测结论。

测试策略：先检查本机 JDK/Maven；使用 Maven wrapper 与便携工具链（如有必要），不改变机器全局设置。只执行明确筛选的单元测试，排除 `*IT`、`@SpringBootTest` 等连接外部服务的测试。若工具链或依赖下载不可用，记录具体障碍，并完成能执行的静态审查，不伪造通过结果。

## 实施记录

- 2026-09-06：工作区干净，当前分支 `agentRag`。规格与计划先于业务代码建立。
- 2026-09-06：实现完成。最终限定回归 87/87 通过（web-app 83，web-admin 4），31 个测试类。真实 Broker IT 仅编译，未运行；迁移 SQL 未执行。
- 记录：`../../ai-rental-agent/mq-closed-loop.md` 与 `../../ai-rental-agent/verification-2026-09-06.md`。实际基础设施验收和既有队列升级仍是部署前工作，不包含在以上勾选的“代码与隔离验证完成”中。
