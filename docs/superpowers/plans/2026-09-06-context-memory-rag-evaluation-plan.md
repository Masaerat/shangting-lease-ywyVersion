# 分层上下文记忆与 RAG 评测实施计划

对应规格：`../specs/2026-09-06-context-memory-rag-evaluation-spec.md`。

- [x] P0：确认 `agentRag` 分支、运行数据和未提交改动，先完成规格与计划。
- [x] P1：实现版本化对话记忆信封、旧格式迁移、确定性状态提取、滚动压缩和上下文预算。
- [x] P2：接入聊天主链路，补齐分层记忆、修正覆盖、并发条带锁、缓存降级测试。
- [x] P3：把混合召回改为 RRF 融合，增加稳定排序、真实模式标识、失败日志和入住知识。
- [x] P4：建立 JSONL 黄金数据集与可复用指标计算器，覆盖同义问题、无答案和模式分布。
- [x] P5：运行限定单测与后端编译，修复回归，生成实际验证记录和使用说明。
- [x] P6：审查差异，更新实施记录；不提交、不推送，除非用户另行明确要求。

测试策略：纯单元测试不连接外部服务；真实向量评测使用独立说明和显式配置，不能混入默认测试。当前 PGVector 为 0 条，因此本轮默认测试验证算法和本地基线，不声称真实 Embedding 指标。

## 实施记录

- 新增 Redis 版本化分层记忆，兼容旧 JSON 数组/换行格式；结构化条件、滚动摘要与最近原文共同组成上下文。
- 聊天链路完成接入；同一 JVM 使用会话条带锁，Redis 故障时安全降级，最新用户修正覆盖旧条件。
- 混合检索使用加权 RRF，增加稳定排序、分类小幅加权与 `HYBRID/VECTOR/LOCAL/EMPTY` 真实来源标识。
- 新增 30 条 V1 JSONL 检索集、指标计算器和 `/app/ai/rag/search` 只读诊断接口。
- Docker Maven 定向回归 22/22 通过；`web-app` 非集成测试 92/92 通过；`web-admin` 知识切块/入库测试 4/4 通过；六模块后端编译成功；`git diff --check` 无空白错误。
- 实现完成时先保持在工作区等待审查；后续仅在用户明确确认后提交、推送并合入 `master`。
